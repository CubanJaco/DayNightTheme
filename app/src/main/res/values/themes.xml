<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Tema por default de los botones-->
        <item name="materialButtonStyle">@style/Button.Colored</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Button.Colored" parent="@style/Widget.MaterialComponents.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="rippleColor">@color/gray_light</item>
        <item name="backgroundTint">@color/colorAccent</item>
        <item name="cornerRadius">@dimen/corner_radius</item>
    </style>

    <style name="TextLabel.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox" />

    <dimen name="corner_radius">10dp</dimen>
</resources>
